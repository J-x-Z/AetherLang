extern "C" {
    fn puts(s: *u8) -> i32;
}

struct Counter {
    val: i64,
}

impl Counter {
    fn new() -> Counter {
        Counter { val: 0 }
    }
}

fn main() {
    let c: Counter = Counter::new();
    puts("Counter::new() works!");
}
